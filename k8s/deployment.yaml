apiVersion: apps/v1
kind: Deployment
metadata:
  name: screencast-recorder
  namespace: statex-apps
  labels:
    app: screencast-recorder
    managed-by: k8s-migration
spec:
  replicas: 1
  selector:
    matchLabels:
      app: screencast-recorder
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  template:
    metadata:
      labels:
        app: screencast-recorder
    spec:
      containers:
        - name: app
          image: localhost:5000/screencast-recorder:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 3391
              name: http
          envFrom:
            - configMapRef:
                name: screencast-recorder-config
            - secretRef:
                name: screencast-recorder-secret
          livenessProbe:
            httpGet:
              path: /health
              port: 3391
            initialDelaySeconds: 15
            periodSeconds: 30
            failureThreshold: 3
            timeoutSeconds: 5
          readinessProbe:
            httpGet:
              path: /health
              port: 3391
            initialDelaySeconds: 10
            periodSeconds: 10
            failureThreshold: 3
            timeoutSeconds: 5
          resources:
            requests:
              memory: "128Mi"
              cpu: "50m"
            limits:
              memory: "512Mi"
              cpu: "500m"
